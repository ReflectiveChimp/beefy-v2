import { css } from '@repo/styles/css';

export const styles = {
  top: css.raw({
    backgroundColor: 'background.header',
  }),
  vaultContainer: css.raw({
    paddingTop: '32px',
    paddingBottom: '32px',
  }),
};

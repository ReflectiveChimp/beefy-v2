import { css } from '@repo/styles/css';

export const styles = {
  tabs: css.raw({
    marginTop: '16px',
    backgroundColor: 'transparent',
    sm: {
      marginTop: '0',
    },
  }),
  select: css.raw({
    width: '100%',
    backgroundColor: 'background.content',
  }),
};

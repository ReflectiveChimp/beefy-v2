import { css } from '@repo/styles/css';

export const styles = {
  container: css.raw({
    backgroundColor: 'background.content.dark',
    borderRadius: '12px 12px 0 0',
    padding: '24px',
  }),
};

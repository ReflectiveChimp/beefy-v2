import { css } from '@repo/styles/css';

export const styles = {
  actions: css.raw({
    display: 'flex',
    flexWrap: 'wrap',
    marginTop: '8px',
    rowGap: '8px',
    columnGap: '8px',
  }),
};

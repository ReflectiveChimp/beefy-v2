import { css } from '@repo/styles/css';

export const styles = {
  title: css.raw({
    textStyle: 'h2',
    color: 'text.light',
    margin: '0',
  }),
  subtitle: css.raw({
    textStyle: 'subline',
    color: 'extracted2029',
  }),
};
